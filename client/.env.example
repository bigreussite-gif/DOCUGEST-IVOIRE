# Développement local : API Express sur le port 4000
VITE_API_BASE_URL=http://localhost:4000
VITE_APP_URL=http://localhost:5173

# Production (build Vercel) : ne pas définir VITE_API_BASE_URL ni VITE_APP_URL
# → le client utilise la même origine que le site (fetch vers /api/...)
